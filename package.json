{
  "name": "ehr-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.4.0",
  "engines": {
    "bun": ">=1.4.0"
  },
  "workspaces": [
    "packages/backend",
    "packages/frontend",
    "packages/shared",
    "packages/workers"
  ],
  "scripts": {
    "dev": "bun --filter \"*\" dev",
    "build": "bun --filter \"*\" build",
    "lint": "bun --filter \"*\" lint",
    "test": "bun --filter \"*\" test"
  },
  "devDependencies": {
    "elysia": "^1.1.20",
    "@elysiajs/eden": "^1.1.3",
    "esbuild": "^0.28.0",
    "typescript": "^5.7.3"
  }
}
