{
  "name": "ehr-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/backend",
    "packages/frontend"
  ],
  "scripts": {
    "dev": "bun --filter \"*\" dev",
    "build": "bun --filter \"*\" build",
    "lint": "bun --filter \"*\" lint",
    "test": "bun --filter \"*\" test"
  },
  "devDependencies": {
    "elysia": "^1.1.20",
    "@elysiajs/eden": "^1.1.3"
  }
}
