AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: EHR Annotation Tool Backend SAM Template

Globals:
  Function:
    Timeout: 30
    MemorySize: 256
    Runtime: nodejs20.x
    Architectures:
      - x86_64

Parameters:
  DocumentsBucketName:
    Type: String
    Description: Name of the S3 bucket for documents
  EhrTableName:
    Type: String
    Description: Name of the single DynamoDB table for EHR data
  GroqApiKey:
    Type: String
    Description: Groq API Key
  NotificationEmail:
    Type: String
    Description: Email address to receive DDoS alert notifications
  DdosRequestThreshold:
    Type: Number
    Default: 200
    Description: Number of Lambda requests in 1 minute to trigger the kill switch
  EhrApiKeyVal:
    Type: String
    Description: API Key for Hono authentication

Resources:
  EhrBackendFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: dist/
      Handler: serverless-bundle.handler
      Timeout: 5
      ReservedConcurrentExecutions: 50 # Prevent run-away compute costs
      FunctionUrlConfig:
        AuthType: NONE
      Environment:
        Variables:
          GROQ_API_KEY: !Ref GroqApiKey
          EHR_TABLE_NAME: !Ref EhrTableName
          DOCUMENTS_BUCKET_NAME: !Ref DocumentsBucketName
          ANNOTATION_QUEUE_URL: !Ref EhrAnnotationQueue
          API_KEY: !Ref EhrApiKeyVal
      Policies:
        - S3ReadPolicy:
            BucketName: !Ref DocumentsBucketName
        - DynamoDBCrudPolicy:
            TableName: !Ref EhrTableName
        - Statement:
            - Effect: Allow
              Action:
                - dynamodb:Query
                - dynamodb:Scan
              Resource: !Sub "arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${EhrTableName}/index/*"
        - SQSSendMessagePolicy:
            QueueName: !GetAtt EhrAnnotationQueue.QueueName

  EhrAnnotationWorkerFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: dist/
      Handler: worker-bundle.handler
      ReservedConcurrentExecutions: 2 # Hard cap on LLM parallel processing
      Events:
        SQSEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt EhrAnnotationQueue.Arn
            BatchSize: 5
            FunctionResponseTypes:
              - ReportBatchItemFailures
      Environment:
        Variables:
          GROQ_API_KEY: !Ref GroqApiKey
          EHR_TABLE_NAME: !Ref EhrTableName
          DOCUMENTS_BUCKET_NAME: !Ref DocumentsBucketName
      Policies:
        - S3ReadPolicy:
            BucketName: !Ref DocumentsBucketName
        - DynamoDBCrudPolicy:
            TableName: !Ref EhrTableName
        - Statement:
            - Effect: Allow
              Action:
                - dynamodb:Query
                - dynamodb:Scan
              Resource: !Sub "arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${EhrTableName}/index/*"

  EhrAnnotationQueue:
    Type: AWS::SQS::Queue
    Properties:
      VisibilityTimeout: 180
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt EhrAnnotationDLQ.Arn
        maxReceiveCount: 3

  EhrAnnotationDLQ:
    Type: AWS::SQS::Queue

  EhrAnnotationQueuePolicy:
    Type: AWS::SQS::QueuePolicy
    Properties:
      Queues:
        - !Ref EhrAnnotationQueue
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: events.amazonaws.com
            Action: sqs:SendMessage
            Resource: !GetAtt EhrAnnotationQueue.Arn

  EhrAnnotationBusRule:
    Type: AWS::Events::Rule
    Properties:
      Description: "Routes S3 ObjectCreated events to the Annotation Queue"
      EventPattern:
        source:
          - aws.s3
        detail-type:
          - Object Created
        detail:
          bucket:
            name:
              - !Ref DocumentsBucketName
          object:
            key:
              - prefix: "documents/"
      State: ENABLED
      Targets:
        - Arn: !GetAtt EhrAnnotationQueue.Arn
          Id: "EhrAnnotationQueueTarget"

  EhrTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Ref EhrTableName
      AttributeDefinitions:
        - AttributeName: PK
          AttributeType: S
        - AttributeName: SK
          AttributeType: S
      KeySchema:
        - AttributeName: PK
          KeyType: HASH
        - AttributeName: SK
          KeyType: RANGE
      GlobalSecondaryIndexes:
        - IndexName: SKIndex
          KeySchema:
            - AttributeName: SK
              KeyType: HASH
            - AttributeName: PK
              KeyType: RANGE
          Projection:
            ProjectionType: ALL
          ProvisionedThroughput:
            ReadCapacityUnits: 5
            WriteCapacityUnits: 5
      BillingMode: PROVISIONED
      ProvisionedThroughput:
        ReadCapacityUnits: 5
        WriteCapacityUnits: 5

  DocumentsBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Ref DocumentsBucketName
      NotificationConfiguration:
        EventBridgeConfiguration:
          EventBridgeEnabled: true
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true



  EhrKillSwitchTopic:
    Type: AWS::SNS::Topic
    Properties:
      TopicName: !Sub "EhrKillSwitchTopic-${AWS::StackName}"
      Subscription:
        - Endpoint: !Ref NotificationEmail
          Protocol: email

  EhrLambdaTrafficAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmDescription: !Sub "Triggered if backend Lambda requests (Invocations + Throttles) exceed ${DdosRequestThreshold} in 1 minute. Triggers Kill Switch."
      EvaluationPeriods: 1
      Threshold: !Ref DdosRequestThreshold
      ComparisonOperator: GreaterThanOrEqualToThreshold
      AlarmActions:
        - !Ref EhrKillSwitchTopic
      TreatMissingData: notBreaching
      Metrics:
        - Id: e1
          Expression: m1 + m2
          Label: TotalRequests
          ReturnData: true
        - Id: m1
          MetricStat:
            Metric:
              Namespace: AWS/Lambda
              MetricName: Invocations
              Dimensions:
                - Name: FunctionName
                  Value: !Ref EhrBackendFunction
            Period: 60
            Stat: Sum
          ReturnData: false
        - Id: m2
          MetricStat:
            Metric:
              Namespace: AWS/Lambda
              MetricName: Throttles
              Dimensions:
                - Name: FunctionName
                  Value: !Ref EhrBackendFunction
            Period: 60
            Stat: Sum
          ReturnData: false

  EhrLambdaKillSwitchFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: dist/
      Handler: kill-switch-bundle.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 128
      Environment:
        Variables:
          BACKEND_FUNCTION_NAME: !Ref EhrBackendFunction
      Policies:
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - lambda:PutFunctionConcurrency
                - lambda:DeleteFunctionConcurrency
              Resource: !GetAtt EhrBackendFunction.Arn
      Events:
        SnsEvent:
          Type: SNS
          Properties:
            Topic: !Ref EhrKillSwitchTopic

Outputs:
  ApiEndpoint:
    Description: "Lambda Function URL"
    Value: !GetAtt EhrBackendFunctionUrl.FunctionUrl

  EhrAnnotationQueueUrl:
    Description: "URL of the Annotation SQS Queue"
    Value: !Ref EhrAnnotationQueue

  EhrAnnotationDLQUrl:
    Description: "URL of the Dead Letter Queue"
    Value: !Ref EhrAnnotationDLQ
