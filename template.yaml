AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: EHR Annotation Tool Backend SAM Template

Globals:
  Function:
    Timeout: 30
    MemorySize: 512
    Runtime: nodejs20.x
    Architectures:
      - x86_64

Parameters:
  DocumentsBucketName:
    Type: String
    Description: Name of the S3 bucket for documents
  DocumentsTableName:
    Type: String
    Description: Name of the DynamoDB table for documents
  AnnotationsTableName:
    Type: String
    Description: Name of the DynamoDB table for annotations
  GroqApiKey:
    Type: String
    Description: Groq API Key

Resources:
  EhrApiGateway:
    Type: AWS::Serverless::Api
    Properties:
      StageName: prod
      Cors:
        AllowMethods: "'*'"
        AllowHeaders: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
        AllowOrigin: "'*'"
      MethodSettings:
        - HttpMethod: '*'
          ResourcePath: '/*'
          ThrottlingBurstLimit: 2
          ThrottlingRateLimit: 5

  EhrBackendFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: dist/
      Handler: serverless-bundle.handler
      ReservedConcurrentExecutions: 5 # Prevent run-away compute costs
      Events:
        ProxyApiRoot:
          Type: Api
          Properties:
            RestApiId: !Ref EhrApiGateway
            Path: /
            Method: ANY
        ProxyApiGreedy:
          Type: Api
          Properties:
            RestApiId: !Ref EhrApiGateway
            Path: /{proxy+}
            Method: ANY
      Environment:
        Variables:
          GROQ_API_KEY: !Ref GroqApiKey
          DOCUMENTS_TABLE_NAME: !Ref DocumentsTableName
          ANNOTATIONS_TABLE_NAME: !Ref AnnotationsTableName
          DOCUMENTS_BUCKET_NAME: !Ref DocumentsBucketName
      Policies:
        - S3ReadPolicy:
            BucketName: !Ref DocumentsBucketName
        - DynamoDBCrudPolicy:
            TableName: !Ref DocumentsTableName
        - DynamoDBCrudPolicy:
            TableName: !Ref AnnotationsTableName
        - SQSSendMessagePolicy:
            QueueName: !GetAtt EhrAnnotationQueue.QueueName

  EhrAnnotationWorkerFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: dist/
      Handler: worker-bundle.handler
      ReservedConcurrentExecutions: 2 # Hard cap on LLM parallel processing
      Events:
        SQSEvent:
          Type: SQS
          Properties:
            Queue: !GetAtt EhrAnnotationQueue.Arn
            BatchSize: 5
      Environment:
        Variables:
          GROQ_API_KEY: !Ref GroqApiKey
          DOCUMENTS_TABLE_NAME: !Ref DocumentsTableName
          ANNOTATIONS_TABLE_NAME: !Ref AnnotationsTableName
          DOCUMENTS_BUCKET_NAME: !Ref DocumentsBucketName
      Policies:
        - S3ReadPolicy:
            BucketName: !Ref DocumentsBucketName
        - DynamoDBCrudPolicy:
            TableName: !Ref DocumentsTableName
        - DynamoDBCrudPolicy:
            TableName: !Ref AnnotationsTableName

  EhrAnnotationQueue:
    Type: AWS::SQS::Queue
    Properties:
      VisibilityTimeout: 180
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt EhrAnnotationDLQ.Arn
        maxReceiveCount: 3

  EhrAnnotationDLQ:
    Type: AWS::SQS::Queue

  EhrAnnotationQueuePolicy:
    Type: AWS::SQS::QueuePolicy
    Properties:
      Queues:
        - !Ref EhrAnnotationQueue
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: events.amazonaws.com
            Action: sqs:SendMessage
            Resource: !GetAtt EhrAnnotationQueue.Arn

  EhrAnnotationBusRule:
    Type: AWS::Events::Rule
    Properties:
      Description: "Routes S3 ObjectCreated events to the Annotation Queue"
      EventPattern:
        source:
          - aws.s3
        detail-type:
          - Object Created
        detail:
          bucket:
            name:
              - !Ref DocumentsBucketName
          object:
            key:
              - prefix: "documents/"
      State: ENABLED
      Targets:
        - Arn: !GetAtt EhrAnnotationQueue.Arn
          Id: "EhrAnnotationQueueTarget"

  DocumentsTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Ref DocumentsTableName
      AttributeDefinitions:
        - AttributeName: id
          AttributeType: S
      KeySchema:
        - AttributeName: id
          KeyType: HASH
      BillingMode: PROVISIONED
      ProvisionedThroughput:
        ReadCapacityUnits: 5
        WriteCapacityUnits: 5

  AnnotationsTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Ref AnnotationsTableName
      AttributeDefinitions:
        - AttributeName: annotationId
          AttributeType: S
        - AttributeName: documentId
          AttributeType: S
      KeySchema:
        - AttributeName: annotationId
          KeyType: HASH
      GlobalSecondaryIndexes:
        - IndexName: DocumentIdIndex
          KeySchema:
            - AttributeName: documentId
              KeyType: HASH
          Projection:
            ProjectionType: ALL
          ProvisionedThroughput:
            ReadCapacityUnits: 5
            WriteCapacityUnits: 5
      BillingMode: PROVISIONED
      ProvisionedThroughput:
        ReadCapacityUnits: 5
        WriteCapacityUnits: 5

  DocumentsBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Ref DocumentsBucketName
      NotificationConfiguration:
        EventBridgeConfiguration:
          EventBridgeEnabled: true
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true

Outputs:
  ApiEndpoint:
    Description: "API Gateway endpoint URL"
    Value: !Sub "https://${EhrApiGateway}.execute-api.${AWS::Region}.amazonaws.com/prod/"

  EhrAnnotationQueueUrl:
    Description: "URL of the Annotation SQS Queue"
    Value: !Ref EhrAnnotationQueue

  EhrAnnotationDLQUrl:
    Description: "URL of the Dead Letter Queue"
    Value: !Ref EhrAnnotationDLQ
